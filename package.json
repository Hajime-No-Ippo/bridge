{
  "name": "opencode-telegram-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mirror a live opencode session to Telegram",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.6.0"
  }
}
